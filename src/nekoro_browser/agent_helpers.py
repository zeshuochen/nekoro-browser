"""agent_helpers.py — Runtime-editable helpers.

Edit this file and call reload_agent_helpers() — no daemon restart needed.
Functions here are merged into the exec namespace alongside helpers.py.

Example:
    async def my_custom_func(daemon):
        return await js(daemon, "document.title")
"""

# Add your custom helpers below. Async functions get `daemon` as first arg.
